#!/bin/bash

curl https://api.github.com/notifications?access_token=TOKEN > ~/.github_notifier/api-response.json

NOTIF=$(cat ~/.github_notifier/api-response.json | jq '.[] | length')

if [[ "$NOTIF" -eq 0 ]]
then
    printf "0"
else
    printf "1"
fi

